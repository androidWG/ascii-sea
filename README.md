# interactive ascii sea

## build

since this uses Vite, you'll need Node.js. it compiles to a static website on the `dist/` folder.

```bash
git clone https://github.com/androidWG/ascii-sea
cd ascii-sea
npm install
npm run build
```